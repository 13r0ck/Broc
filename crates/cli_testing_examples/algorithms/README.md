# Algorithm examples

To run:

```bash
cargo run fibonacci.broc
cargo run quicksort.broc
```

To run in release mode instead, do:

```bash
cargo run --release fibonacci.broc
cargo run --release quicksort.broc
```
